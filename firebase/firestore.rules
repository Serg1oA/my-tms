rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ponytail: app uses Admin SDK on the server; rules block direct client writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
